import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF0F4F8); // Soft light gray-blue
  static const Color primary = Color(0xFF4A6CF7);     // Indigo blue (main color)
  static const Color secondary = Color(0xFFB0BEC5);   // Soft gray for text/labels
  static const Color accent = Color(0xFF00C896);      // Greenish for positive actions
  static const Color textDark = Color(0xFF263238);    // Deep gray for primary text
  static const Color card = Color(0xFFFFFFFF);        // Card background
  static const Color border = Color(0xFFE0E0E0);       // Card border or divider
}
